apiVersion: v1
kind: Service
metadata:
  name: todobackend-svc
spec:
  type: NodePort
  selector:
      app: todobackend
  ports:
      - name: http
        nodePort: 30080
        protocol: TCP
        port: 1234
        targetPort: 3000